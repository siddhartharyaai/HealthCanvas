import React from 'react'
import ReactDOM from 'react-dom/client'
import HealthCanvas from './HealthCanvas'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <HealthCanvas />
  </React.StrictMode>
)
